app.controller('dashboardController', function ($scope, $rootScope, $routeParams, $location, $http, Data) {
    //initially set those objects to null to avoid undefined error
    $scope.comboNames = ["Emil", "Tobias", "Linus"];

});
